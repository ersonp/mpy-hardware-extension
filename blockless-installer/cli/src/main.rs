// Subcommands land in commit 12 (ops.rs + cli wiring). This stub only proves the
// workspace links against blockless-installer-core.
fn main() {
    println!("blockless-installer {}", env!("CARGO_PKG_VERSION"));
}
