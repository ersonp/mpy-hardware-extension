# bundled extension fallback (not tracked; supplied at run time via --vsix)
assets/*.vsix

# logs / local scratch
*.log

# Rust build output (Cargo.lock IS committed; this is a binary workspace)
target/
